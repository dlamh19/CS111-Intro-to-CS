/*

public static int factorial(int n)
{
	if(n == 1)				//base case
		return 1;
	int f = n * factorial(n-1);
	return f;	

}
faster to write recursive but iterative is faster than recursive

public static int bunnyEars(int n)
{
	if (n==0)
		return 0

	if(n%2 == 0)
		return 2 + bunnyEars(n-1);
	if(n%2 == 1)
		return 3 + bunnyEars(n-1);

}
	
Recursive 
-calls itself on a smaller portion of the same roblem to find a solution to the problem
-base case: stops the recursive action 
-recursive case: responsible for iterating or recursion




*/
